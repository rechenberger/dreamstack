overwrite: true
schema:
  - ${NEXT_PUBLIC_GRAPHQL_API}:
      # headers:
      #   'x-hasura-admin-secret': ${HASURA_ADMIN_SECRET}
generates:
  ./generated/graphql.schema.json:
    plugins:
      - 'introspection'
  ./generated/graphql.min.schema.json:
    plugins:
      - 'introspection'
    config:
      minify: true
